name = "tests"
description = "Flux tests"
version = "0.1.0-dev"

executables = ["tests"]

[[depends-on]]
flux = "*"
flux_benchmark = "*"
stopwatch = "~0.1.0"

[[pins]]
flux = { path='..' }
flux_benchmark = { path='../flux_benchmark' }
